import React from "react";
import styles from './RequestButton.module.css'

const RequestButton = () => {
    return <button className={styles.requestButton}>Request Beta</button>
}

export default RequestButton;